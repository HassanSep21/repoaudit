# See 05_WORKFLOW.md for full details on each of these.

# Required once Build Plan Phase 5 (Semantic Analysis) starts
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Effectively required (D21) — unauthenticated GitHub API is 60 req/hr, exhausted fast
GITHUB_TOKEN=

# Local dev: leave unset, defaults to SQLite.
# Deployed (Render): set to a free-tier Supabase Postgres connection string (D14).
DATABASE_URL=
